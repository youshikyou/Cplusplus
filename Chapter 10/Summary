/*
10 chapter
1.类和结构唯一的区别是，结构默认访问类型是public，类为private，通常使用类来实现类描述，把结构限制为纯粹的数据对象。
2.内联函数的特殊规则是在每个使用它的文件中都对其进行定义，确保内联函数对多文件程序中的所有文件都可用，最简便的方法是
将内联定义放在定义类的头文件中。
3.设计类时，通常应提供对所有类成员做隐式初始化的默认构造函数。
4.隐式地调用默认构造函数时，不要使用圆括号。P355,例子
5.通常不应在代码中显式地调用析构函数。
6.Stock stock2 = Stock("boffo objects",2,2.0)
Stock stock1("NanoSmart",12,20.0)
第一种是初始化，创建了一个临时对象（也可能不会），然后将这个临时的对象赋值给stock2，然后析构函数会删除这个临时对象
第二种是对象的直接初始化。
7.没有定义任何构造函数时候，编译器提供构造函数，如果定义了构造函数，就必须同时定义默认构造函数，否则将出错。
即要不定义两个构造函数（默认的和自定义的），要不就不定义。
8.析构函数的出现参看实例 P360
9.确保函数不会修改调用对象，将const 关键字放在函数括号后面。
void show（） const；
函数定义
void stock：：show() const
10.P114 类和结构体用箭头运算符
*/
