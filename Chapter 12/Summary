不能在类声明中初始化静态成员变量
类中包含使用new初始化的指针成员，应当定义复制构造函数，以复制指向的数据，而不是指针，即深度复制。
试图删除已经删除的数据导致的结果是不确定的，可能改变内存的内容，导致程序异常终止。
P464,构造函数初始化参数，只有构造函数才能使用初始化列表语法，对const类成员，必须使用这种语法。
strcat_s函数中，第二个参数是合并之后的数量，而strcmp_s的第二个参数是拷贝的数量。
使用string类， 记得::tolower前面有::,　而且是::tolower，不是::tolower()
char数组 记得是 s[i]=tolower(s[i])
