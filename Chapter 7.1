
#include <iostream>
#include <vector>
#include<array>
#include <cstring>
#include<string>
#include<climits>
#include<cctype>
#include<fstream>

using namespace std;
double avg(double, double);

int main() {
	double result=0;
	double a, b;
	cout << "please input 2 numbers(0 to stop): "<<"\n";
	cin >> a >> b;
	while (a != 0 && b !=0)
	{
		result = avg(a, b);
		cout << "average: "<<result << "\n";
		cout << "please input 2 numbers: \n";
		cin >> a >> b;
	}
	
	cout << "bye" << "\n";
  return 0;
}

double avg(double a, double b)
{
	return (2.0*a*b) / (a + b);
}
